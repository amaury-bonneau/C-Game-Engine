// #ifndef TEXTURE_H
// #define TEXTURE_H

// #include <SDL.h>
// #include "player.h"

// SDL_Texture* load_texture_from_path (SDL_Renderer *ren, char *textureKey);
// SDL_Texture* load_texture (SDL_Renderer *ren, char *textureKey);

// #endif